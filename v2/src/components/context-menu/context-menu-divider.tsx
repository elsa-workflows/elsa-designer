import {Host, h} from '@stencil/core';
import {FunctionalComponent} from "@stencil/core/dist/declarations/vdom";

export const ContextMenuDivider: FunctionalComponent = (props, children) => <div class="dropdown-divider"/>;
